# openalgo>=1.0.0   # Removed: migrating to AngelOne SmartAPI

# Phase 1 & 2: AngelOne SmartAPI Integration
smartapi-python==1.3.0    # AngelOne SmartAPI SDK (exact version)
pyotp>=2.8.0              # TOTP code generation for 2FA
requests>=2.31.0          # HTTP client for REST API fallback
websocket-client>=1.6.0   # WebSocket for live market data

# Core dependencies
pandas>=2.2.0
yfinance>=0.2.40
